#version 330
// fragment shader


  varying vec4 color; 
in vec2 TexCoords_;
uniform sampler2D gSampler;

void main() 
{ 
    gl_FragColor = texture2D(gSampler, TexCoords_.xy);
    gl_FragColor =  gl_FragColor * color;
}
